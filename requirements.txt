# Core dependencies
numpy>=1.21.0
opencv-python>=4.5.0
dlib>=19.22.0
mediapipe>=0.8.0

# Web frameworks
flask>=2.0.0
fastapi>=0.68.0
uvicorn>=0.15.0

# Serial communication
pyserial>=3.5

# HTTP requests
requests>=2.26.0

# Data visualization
matplotlib>=3.4.0

# Image processing
Pillow>=8.3.0

# ROS2 dependencies (install via ROS2, but listed for reference)
# rclpy
# sensor_msgs
# std_msgs
# cv_bridge

# Shimmer sensor (for ROS2 shimmer_driver)
pyshimmer>=0.1.0

# Additional utilities
# Note: tkinter is usually included with Python, but may need to be installed separately on some systems
# sudo apt-get install python3-tk

