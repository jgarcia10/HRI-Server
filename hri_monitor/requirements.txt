fastapi>=0.110
uvicorn[standard]>=0.29
numpy>=1.26
opencv-python>=4.9
PyYAML>=6.0
httpx>=0.27
pytest>=8.0
# Pin to a version that still ships the legacy `mp.solutions.face_mesh` API
# (the RGB blink driver uses it). 0.10.20+ dropped `solutions` for the Tasks API.
mediapipe==0.10.14
# dlib-bin is the prebuilt CPU wheel — avoids the source build trying (and failing)
# to compile CUDA support on machines that have the CUDA toolkit but no usable GPU.
dlib-bin>=19.24
pyserial>=3.5
pingouin>=0.6
